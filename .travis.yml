language: cpp
compiler: gcc

os: linux
dist: xenial
sudo: required

addons:
  apt:
    sources:
      - sourceline: 'ppa:duggan/bats'
    packages:
      - git
      - gcc-mipsel-linux-gnu
      - bats # Unit testing in Bash

before_install:
  - sudo bash README.Debian

script:
  - cd code
  - make
  - cd build
  - bats -p ../test/test_all.bats